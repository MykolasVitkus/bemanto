{% extends 'base.html.twig' %}

{% block title %} {{ pageTitle }} {% endblock %}

{% block stylesheets %}

    {# {{ encore_entry_link_tags('user_settings') }} #}

{% endblock %}

{% block body %}

    <div style="align-center" class="container col-xs-12 col-md-10 mt-5 bg-white">
        <div class="row col-xs-12 col-md-8">
            <div class="col justify-content-center align-items-center">
                <h2>Kategorijos redagavimas</h2><br>

                {% if errorTitle %}

                    <div class = "alert alert-{{ errorType }}">
                        <strong>{{ errorTitle }}</strong> {{ errorMessage }}
                    </div>

                {% endif %}

                {{ form_start(edit_form) }}
                
                    {{ form_errors(edit_form.name) }}
                    {{ form_widget(edit_form.name) }}

                    <div class="text-center">
                        <button class="btn btn-success mt-4" type="submit">Redaguoti kategoriją</button> 
                    </div>

                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}

    {# {{ encore_entry_script_tags('account_settings') }} #}

{% endblock %}
